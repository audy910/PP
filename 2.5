
import java.util.Scanner;

public class names
{
    public static void main(String args[]) 
    {
        String first, last, full;
        int firstLength, lastLength, fullLength;
        
        Scanner keyboard = new Scanner (System.in);
        System.out.println("Enter first name \n then last name: ");
        first = keyboard.nextLine();
        last = keyboard.nextLine();
        full = first + last;
        
        firstLength = first.length();
        lastLength = last.length();
        fullLength = full.length();
        
        System.out.print(first + " = " + firstLength + " letters ") ;       
        System.out.print(last + " = " + lastLength + " letters ");
        System.out.print(full + " = " + fullLength + " letters  \n" );
        
        System.out.println(full.toUpperCase());
        
        
        System.out.println("audrey");
        
    }// End method main
}// End WinPercentage
