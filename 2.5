import java.util.Scanner;

public class names
{
    public static void main(String args[]) 
    {
        String first, last, full;
        int firstLength, lastLength, fullLength;
        
        Scanner keyboard = new Scanner (Syystem.in);
        System.out.print("Enter first name \n then last name: ");
        first = keyboard.nextln();
        last = keyboard.nextln();
        full = first + last;
        
        firstLength = first.length();
        lastLength = last.length();
        fullLength = full.length();
        
        System.out.print(first + " = " first.length() + "letters")        
        System.out.print(last + " = " last.length() + "letters");
        System.out.print(full + " = " + full.length() + "letters" );
        
        
        System.out.println("audrey");
        
    }// End method main
}// End WinPercentage
